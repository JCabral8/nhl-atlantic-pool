{
  "buildCommand": "cd frontend && npm ci && ./node_modules/.bin/vite build",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install --ignore-scripts",
  "rewrites": [
    {
      "source": "/:path((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
