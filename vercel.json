{
  "buildCommand": "cd frontend && npm ci && npm exec vite -- build",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install --ignore-scripts",
  "rewrites": [
    {
      "source": "/:path((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
