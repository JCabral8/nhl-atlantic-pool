# Backend port (use 3002 if 3001 is taken by another app)
PORT=3002

# PostgreSQL: set this to use Postgres (e.g. for local testing without building better-sqlite3 on Windows).
# Get the URL from Railway: Project → PostgreSQL → Connect → "Postgres connection URL"
# DATABASE_URL=postgresql://user:pass@host:port/railway
